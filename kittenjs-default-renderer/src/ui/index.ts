import { Table as table } from './Table'
import { Form as form } from './Form'
import { Layout as layout } from './Layout'
import { Alert as alert } from './Alert'
import { Loading as loading } from './Loading'
import { Button as button } from './Button'
import { Modal as modal } from './Modal'
export {
    table as createTable,
    form as createForm,
    layout as createLayout,
    table as updateTable,
    form as updateForm,
    layout as updateLayout,
    alert as createAlert,
    loading as createLoading,
    alert as updateAlert,
    loading as updateLoading,
    button as createButton,
    button as updateButton,
    modal as createModal,
    modal as updateModal
}
